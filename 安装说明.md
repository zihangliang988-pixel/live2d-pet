# 🐾 桌宠助手 - Python 3.13 安装指南

## ⚠️ 重要说明

由于你使用的是 **Python 3.13.3**，某些库（如 `pyaudio`、`pyautogui`）没有预编译的 wheel 包，需要从源码编译，这会导致：
- ❌ 安装时间很长（可能卡住）
- ❌ 需要编译器（Visual Studio Build Tools）
- ❌ 可能编译失败

## 🚀 推荐安装方案

### 方案 1：最小依赖（推荐，快速）

**只安装核心功能，跳过语音识别：**

```bash
cd D:\桌宠

# 1. 安装核心依赖（无需编译）
pip install PyQt5 SpeechRecognition pyttsx3 ollama requests Pillow

# 2. 运行桌宠
python desktop_pet.py
```

**功能：**
- ✅ 可爱二次元桌宠
- ✅ 拖动、点击、右键菜单
- ✅ 文字输入 + LLM 命令理解
- ✅ 文件管理（打开/创建/删除/移动）
- ❌ 语音识别（需要 pyaudio）

---

### 方案 2：完整功能（需要额外步骤）

**如果你想使用语音识别：**

#### 步骤 1：安装 Visual Studio Build Tools
1. 访问：https://visualstudio.microsoft.com/visual-cpp-build-tools/
2. 下载并安装
3. 选择 "Desktop development with C++"
4. 完成安装（约 2-3GB）

#### 步骤 2：安装 pyaudio
```bash
# 方法 1：使用预编译 wheel（推荐）
pip install https://github.com/corvince/PyAudio-wheels/releases/download/v0.2.14/cp313-cp313-win_amd64.whl

# 方法 2：如果上面的链接失效，从源码编译
pip install pyaudio
```

#### 步骤 3：安装其他依赖
```bash
pip install sounddevice soundfile
```

---

### 方案 3：使用 Conda（最简单）

**如果你安装了 Anaconda/Miniconda：**

```bash
# 创建 Python 3.11 环境（兼容性更好）
conda create -n desktop_pet python=3.11
conda activate desktop_pet

# 安装依赖
conda install pyqt pyaudio
pip install SpeechRecognition pyttsx3 ollama
```

---

## 📦 依赖说明

| 包名 | 用途 | 必需 | 备注 |
|------|------|------|------|
| **PyQt5** | 界面 | ✅ | 核心依赖 |
| **SpeechRecognition** | 语音识别 | ⚠️ | 需要 pyaudio |
| **pyaudio** | 音频输入 | ❌ | Python 3.13 难安装 |
| **pyttsx3** | 文字转语音 | ✅ | 离线可用 |
| **ollama** | 大模型客户端 | ✅ | 需要 Ollama |
| **requests** | HTTP 请求 | ✅ | 通用库 |
| **Pillow** | 图像处理 | ✅ | 通用库 |
| **sounddevice** | 音频设备 | ❌ | pyaudio 替代 |

---

## 🎯 快速开始（推荐）

### 1. 安装 Ollama（已完成）
```bash
# 确认 Ollama 已安装
ollama list
```

### 2. 安装 Python 依赖
```bash
cd D:\桌宠
pip install PyQt5 SpeechRecognition pyttsx3 ollama requests Pillow
```

### 3. 运行桌宠
```bash
python desktop_pet.py
```

### 4. 测试功能
- ✅ 拖动桌宠
- ✅ 双击打开助手
- ✅ 输入"打开记事本"
- ✅ 输入"创建 test.txt"

---

## 🔧 如果遇到问题

### 问题 1：pip install 卡住
**解决：**
```bash
# 使用国内镜像
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple PyQt5 pyttsx3 ollama
```

### 问题 2：pyaudio 安装失败
**解决：**
- 跳过语音识别功能
- 使用方案 3（Conda）
- 降级到 Python 3.11

### 问题 3：Ollama 连接失败
**解决：**
```bash
# 检查 Ollama 是否运行
ollama list

# 下载模型
ollama pull llama3.2:3b

# 测试连接
ollama run llama3.2:3b "你好"
```

### 问题 4：桌宠不显示
**解决：**
- 检查是否被其他窗口遮挡
- 尝试调整窗口位置
- 查看控制台错误信息

---

## 💡 最佳实践

**对于 Python 3.13 用户：**

1. **优先使用方案 1**（最小依赖）
   - 安装快，稳定性好
   - 核心功能完整
   - 文字输入 + LLM 足够智能

2. **语音识别可选**
   - 如果必须使用，建议降级到 Python 3.11
   - 或使用 Conda 环境

3. **Ollama 是核心**
   - 确保 Ollama 正常运行
   - 这是桌宠智能的关键

---

## 📝 总结

**推荐配置：**
- Python 3.13 + PyQt5 + pyttsx3 + Ollama
- 跳过 pyaudio，使用文字输入
- 功能完整，安装简单

**如果必须语音识别：**
- 使用 Python 3.11 或 Conda
- 或安装 Visual Studio Build Tools

---

**现在运行以下命令开始：**
```bash
cd D:\桌宠
pip install PyQt5 SpeechRecognition pyttsx3 ollama requests Pillow
python desktop_pet.py
```
