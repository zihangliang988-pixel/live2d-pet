# 桌宠项目依赖 - Python 3.13 兼容版
# 已移除需要编译的包

# GUI 框架
PyQt5>=5.15.0
PyQtWebEngine>=5.15.0

# 语音识别（需要网络）
SpeechRecognition>=3.10.0

# 文字转语音（离线，无需编译）
pyttsx3>=2.90

# 大模型客户端
ollama>=0.1.0

# 其他工具
requests>=2.28.0
Pillow>=10.0.0

# 可选：如果需要语音识别，安装 pyaudio
# 如果 pip install pyaudio 失败，请使用以下替代方案：
# conda install pyaudio
# 或者使用 sounddevice + soundfile
sounddevice>=0.5.0
soundfile>=0.13.0
