# 🐾 桌宠助手 - 使用指南

## 🎯 快速开始

### 第 1 步：安装 Ollama（大模型）

**方法 1：使用安装脚本（推荐）**
```bash
# 双击运行
setup_ollama.bat
```

**方法 2：手动安装**
1. 访问 https://ollama.com/download/windows
2. 下载并安装 Ollama
3. 打开命令行，运行：
   ```bash
   ollama pull llama3.2:3b
   ```

### 第 2 步：安装 Python 依赖

```bash
cd D:\桌宠
pip install -r requirements.txt
```

### 第 3 步：运行桌宠

```bash
python desktop_pet.py
```

---

## 🎮 功能说明

### 基本操作

| 操作 | 说明 |
|------|------|
| 左键拖动 | 移动桌宠位置 |
| 左键单击 | 桌宠显示开心表情 |
| 左键双击 | 打开助手对话框 |
| 右键单击 | 显示功能菜单 |

### 语音命令

**支持的命令：**

1. **打开文件/程序**
   - "打开记事本"
   - "启动 Chrome"
   - "进入 游戏"
   - "运行 Python"

2. **创建文件**
   - "创建 test.txt"
   - "新建 文档.doc"
   - "建一个 图片.png"

3. **删除文件**
   - "删除 file.txt"
   - "删掉 文件夹"
   - "删了 旧文件"

4. **移动文件**
   - "移动 file.txt 到 D:/backup"
   - "移到 桌面"

5. **查看文件**
   - "查看 file.txt"
   - "看看 图片.png"
   - "显示 信息"

---

## 🤖 大模型说明

### 使用的模型

- **模型名称**: Llama 3.2 (3B)
- **大小**: 约 2GB
- **特点**: 
  - 完全免费，离线可用
  - 响应速度快
  - 中文理解良好
  - 隐私保护（本地运行）

### 工作原理

1. 用户输入自然语言命令
2. Llama 3.2 解析命令意图
3. 转换为结构化操作（JSON）
4. 执行对应文件操作

### 示例

**输入**: "帮我打开桌面上的记事本"
**LLM 解析**:
```json
{
  "action": "open",
  "target": "记事本",
  "message": "正在打开记事本..."
}
```
**执行**: 调用系统打开记事本

---

## ⚙️ 配置说明

### 配置文件 (config.json)

```json
{
  "user_preferences": {
    "desktop_path": "C:/Users/你的用户名/Desktop",
    "default_folder": "D:/桌宠/文件",
    "confirmation_required": ["delete", "move", "rename"]
  }
}
```

### 修改配置

1. 编辑 `config.json`
2. 修改 `desktop_path` 为你的桌面路径
3. 修改 `default_folder` 为文件保存位置

---

## 🐛 常见问题

### Q1: 桌宠不显示？
**A**: 检查是否被其他窗口遮挡，桌宠默认置顶显示。

### Q2: 语音识别失败？
**A**: 
- 检查麦克风是否正常工作
- 确保安装了 `pyaudio` 和 `SpeechRecognition`
- 需要网络连接（使用 Google 语音识别）

### Q3: 大模型解析失败？
**A**: 
- 检查 Ollama 是否正在运行
- 确认已下载模型：`ollama list`
- 重启 Ollama 服务

### Q4: 打开文件夹失败？
**A**: 
- 检查路径是否正确
- 确保有权限访问该文件夹
- 文件夹不存在时会自动创建

### Q5: 模型响应慢？
**A**: 
- 首次运行需要加载模型（约 10-30 秒）
- 后续运行会快很多
- 如果仍然慢，可以换用更小的模型

---

## 🎨 自定义

### 更换桌宠形象

编辑 `desktop_pet.py` 中的 `CutePetPainter` 类，修改：
- 头发颜色：`#FFD700` (金黄色)
- 衣服颜色：`#FFB6C1` (粉色)
- 眼睛颜色：`#4169E1` (蓝色)

### 更换大模型

编辑 `llm_parser.py`，修改：
```python
class LLMCommandParser:
    def __init__(self, model_name="llama3.2:3b"):  # 改为其他模型
        self.model_name = model_name
```

**可用模型**:
- `llama3.2:3b` - 快速，2GB
- `qwen2.5:7b` - 更智能，5GB
- `phi3:3.8b` - 平衡，2.5GB

---

## 📝 更新日志

### v3.0 (当前版本)
- ✅ 集成 Ollama 大模型
- ✅ 支持自然语言命令
- ✅ 可爱二次元形象
- ✅ 自动眨眼动画
- ✅ 点击反馈表情

### v2.0
- ✅ 规则匹配命令解析
- ✅ 透明美化菜单
- ✅ 文件管理功能

### v1.0
- ✅ 基础桌宠功能
- ✅ 拖动和右键菜单

---

## 📞 技术支持

如有问题，请检查：
1. Python 版本 >= 3.8
2. 所有依赖已安装
3. Ollama 服务正常运行
4. 配置文件路径正确

**项目地址**: `D:\桌宠`
